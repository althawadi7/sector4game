[/Script/Engine.RendererSettings]
; --- Meta Quest 3 / standalone OpenXR performance baseline ---

; Disabling distance fields avoids requiring a full depth pre-pass, which is slow on standalone XR devices.
r.DistanceFields=0
r.GenerateMeshDistanceFields=0

; Single-pass mobile rendering path required for mobile multi-view VR.
r.Mobile.AllowFramebufferFetch=1
r.Mobile.ShadingPath=0

; PSO precaching is extremely slow on first launch for standalone XR devices.
r.PSOPrecaching=0

; Draw-call stability on Quest 3 (UE 5.5+): keep GPUScene, mobile AO, and occlusion queries off.
r.Mobile.EnableGPUScene=0
r.Mobile.AmbientOcclusion=0
r.AllowOcclusionQueries=0
r.OcclusionFeedback.Enable=0

; Quest OpenXR: dynamic foveated rendering + Vulkan VRS (engine Meta_Quest_3 profile also sets these).
xr.VRS.DynamicFoveation=1
r.Vulkan.VRSFormat=3
r.Vulkan.AllowFDMOffset=1
r.Android.DisableVulkanSupport=0

; Niagara GPU sim can hard-crash on standalone Quest — keep on CPU.
fx.NiagaraAllowGPUParticles=0
FX.AllowGPUSorting=0

; Texture streaming pool tuned for Quest 3 (12 GB RAM class device).
r.Streaming.PoolSize=450
r.Streaming.MaxEffectiveScreenSize=0

; Slightly below native eye buffer for stable 72/90 Hz; raise to 1.0 if you have perf headroom.
r.MobileContentScaleFactor=0.85

; Scalable quality groups — conservative for VR combat + zombies.
sg.ViewDistanceQuality=1
sg.AntiAliasingQuality=1
sg.ShadowQuality=1
sg.GlobalIlluminationQuality=0
sg.ReflectionQuality=0
sg.PostProcessQuality=0
sg.TextureQuality=1
sg.EffectsQuality=1
sg.FoliageQuality=0
sg.LandscapeQuality=0

; VR exposure stability on device (matches level PPV manual bias tuning).
r.DefaultFeature.AutoExposure=0
r.EyeAdaptationQuality=0
r.EyeAdaptation.PreExposureOverride=1.0
r.BloomQuality=0
